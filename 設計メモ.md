# SPEED METER 設計メモ

## 何を作るか
Even G2 に現在の移動速度を km/h で表示する。速度源はスマホ本体の GPS (Web Geolocation API)。

## なぜこの構成か
- カメラや追加センサは G2 に無いので速度ソースはスマホ側しか選べない
- Web Geolocation API は HTTPS 必須で、Even Hub WebView も HTTPS で動くので相性が良い
- 公式 SDK の `@evenrealities/even_hub_sdk` v0.0.10 を直接使う (依存を増やさない)
- `even-better-sdk` も検討したが、1画面1テキストという最小構成では Fluent API の恩恵が薄い

## モジュール構成
| ファイル | 責務 |
|---------|------|
| `src/gps.ts` | 位置購読、速度計算、EMA 平滑、最大/平均/距離の集計 |
| `src/g2.ts` | Bridge 初期化 / TextContainer 作成 / イベント正規化 / Mock fallback |
| `src/main.ts` | 両者を繋ぎ、DOM (デバッグ/Mock 用) にも同じ値を投影 |
| `index.html` | ブラウザ上の簡易ダッシュボード (G2 非接続時も何か見える) |

## GPS 設計の要点
### 速度ソース
1. `coords.speed` が有効ならそれを使う (`source: 'native'`)
2. null / NaN のときは前回位置との `haversine(m) / dt(s)` で推定 (`source: 'derived'`)
3. 初回 or 差分計算に失敗した場合は 0 を出す (`source: 'idle'`)

iOS Safari の `coords.speed` は停止時や取り始めに null を返すことがあるので、上記フォールバックが実用上は重要。

### 平滑
EMA α=0.35。強すぎるとレスポンスが鈍く、弱すぎると km/h の一の位がちらつく。トレードオフ点として中程度に設定。

### 停止判定
0.5 m/s (≒1.8 km/h) 未満は 0 に丸めて "停止中" タグを付ける。GPS の微小揺らぎで止まっているのに数値が跳ねるのを防ぐ。

### 精度フィルタ
`coords.accuracy > 50m` は屋内や衛星不足の可能性が高いので捨てる (ただし初回だけは通す — そうしないと初期表示が永遠に `--` のままになる)。

## G2 表示
### フォントサイズが固定という制約
SDK v0.0.10 には `fontSize` 相当のフィールドが無い。大きく見せたい場合は:
- A. 空行と中央寄せで視覚的に強調 (← 今回採用)
- B. Canvas で数字を描いて PNG 化 → `updateImageRawData` (より大きくできるが複雑・遅い)

まずは A でユーザーがどこまで妥協できるかを見る。不満なら B に差し替え。

### レイアウト
`width: 576, height: 288` の TextContainer 一枚。中身は 24 char 幅を仮定して手動で中央寄せ。実機で確認してから width を調整する。

### イベント
`isEventCapture: 1` を付けたのは TextContainer では通常不要だが、タップ/ダブルタップを拾いたいので必須。

### eventType=0 バグ
SDK から `eventType: undefined` + `containerName: 'speed-main'` だけ来るケースがあるので、`g2.ts::resolveEventType` で CLICK 扱いに正規化。

## Mock / フォールバック戦略
- `waitForEvenAppBridge()` を 4 秒でタイムアウト
- タイムアウトしたら `connected: false` のモックハンドルを返し、DOM 側のみ更新
- `navigator.geolocation` が存在しない環境では `startMock()` を呼んで正弦波で速度を流す (デザイン確認用)

## やらなかったこと
- OBD や外部センサ対応 (スコープ外、G2 スマホ側だけで完結させる)
- バックグラウンド動作 (G2 アプリはフォアグラウンド前提)
- 地図表示 (画面が狭いのと位置情報のプライバシー配慮)
- ユニットテスト (まずは手動での実機確認を優先)

## 将来の拡張候補
- スクロールで「現在速度 / 最大速度 / 距離ログ」の画面切替
- `setLocalStorage` でセッション間の最大値を保存
- `Canvas → PNG` で超大きい数字表示
- `app.json` に supported_languages 別のローカライズ文字列を追加
