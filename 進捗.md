# SPEED METER 進捗

> Even G2 上にスマホGPSの現在速度を km/h で表示するアプリ
> 起点日: 2026-04-19

## 全体状況
- [x] プロジェクト骨格 (package.json / vite / tsconfig / app.json / index.html)
- [x] GPS 速度ロジック (`src/gps.ts`)
- [x] G2 Bridge ラッパ (`src/g2.ts`)
- [x] メインロジック (`src/main.ts`)
- [x] ブラウザ用 DOM UI (mock fallback 兼デバッグビュー)
- [x] QR 発行スクリプト (`scripts/qr.mjs`)
- [x] Mock モード (URL `?mock=1` / ボタン / 自動フォールバック)
- [x] Simulator 起動スクリプト (`scripts/sim.mjs`, `npm run sim`)
- [x] Mock プロファイル (walk / city / highway) + スクロールで切替
- [x] `npm install` 実施 (2026-04-19 / 99 packages, 0 vuln)
- [x] `tsc -b` 通過
- [x] `npm run build` 通過 (dist/index.html 4.24 kB / assets 78.70 kB)
- [x] Simulator 起動確認 (vite :5273 / simulator GUI プロセス確認済)
- [x] `.ehpk` ビルド成功 (`speed-meter.ehpk` = **36.4 KB**)
  - dist/index.html 4.24 KB / dist/assets/index-*.js 81.10 KB (gzip 32.13 KB)
- [x] 実機 (Android + G2) 動作確認 → **致命的問題発覚**

## 🚨 ブロッカー (2026-04-19)
### Even Hub WebView が Web Geolocation を silent deny
- 実機診断結果:
  - `permissions.query: prompt` (一度も許可ダイアログ出てない)
  - `getCurrentPosition: NG code=1 PERMISSION_DENIED, msg="user denied geolocation"`
  - `protocol: http (secure context: true)`
  - Android 設定でアプリ権限は全許可済みの状態
- 典型的な **Android WebView が `WebChromeClient.onGeolocationPermissionsShowPrompt` を実装していない / 常に deny を返す**パターン
- SDK (v0.0.10) の `EvenAppMethod` enum にも **location / geolocation 系 API は無い** (UserInfo / GlassesInfo / Storage / UI / Audio / IMU のみ)
- → **現状の Even Hub では本アプリから実機 GPS は取得不可**
- 対処: Mock モードで UI は動作させる。Even Hub 側の SDK アップデートで Web Geolocation 対応 or location bridge 追加待ち。

## ポート割当
- 既存プロジェクト (onsa 等) と競合を避けるため **5273** に固定 (`strictPort: true`)
- QR / sim / vite すべて 5273 を参照

## Windows の `npm run sim` バグ対策 (2026-04-19)
- 症状: `[sim] 'profile' は、内部コマンドまたは外部コマンド...` が出て sim が即死
- 原因: `spawn(..., { shell: true })` で Windows cmd が URL 内の `&` をコマンド区切りと解釈
- 対策: Windows のみ URL を `"..."` でクォートして渡す (`scripts/sim.mjs`)

## 仕様まとめ
- SDK: `@evenrealities/even_hub_sdk` ^0.0.10
- `permissions` は `location` のみ (app.json)
- 1 TextContainer (ID=1, "speed-main") で 576×288 全面を使う
- `navigator.geolocation.watchPosition({ enableHighAccuracy: true })` を購読
- `position.coords.speed` が null / NaN のときは haversine で前回位置との差分から推定
- EMA (α=0.35) で軽く平滑、0.5 m/s 未満は停止扱い
- 精度 50m 超の測位は (初回以降) 捨てる
- タップ: km/h ⇔ mph 切替 (SDK バグで eventType=0 が undefined になるケースを正規化)
- ダブルタップ: 終了 (`shutDownPageContainer(0)`)

## 画面レイアウト (G2 576×288) — 全部テキスト化 (2026-04-19 v2)
```
┌──────────────────────────────────────────────────┐
│ 04/19 17:00                                      │
│             █████ █████                          │
│                 █     █                          │
│             █████ █████  km/h                    │
│             █         █                          │
│             █████ █████                          │
│ MAX 48   AVG 24                                  │
└──────────────────────────────────────────────────┘
```
3 TextContainer 構成 (画像は使わない):
1. Text `sm-time` (240×36 top-left): `MM/DD HH:MM`
2. Text `sm-speed` (576×200 center): ドット絵大数字 + " km/h" ラベル
3. Text `sm-stats` (bottom-full-width, isEventCapture=1): `MAX xx  AVG yy`

## なぜ画像を捨てたか (2026-04-19 実機で追い付かなかったので変更)
- **BLE 画像送信**: 240×144 1-bit BMP ≈ 4.4 KB → 194 B/packet で 22 packets → 実機で 1–2 秒/枚
  - docs 14.7.1: 「毎秒数回が限界」
- **textContainerUpgrade**: 1 パケット (~100 B) で完結、毎秒複数更新しても余裕
- 大きい数字は `█` (U+2588) の 5×5 ドット絵で代替 (`src/bigText.ts`)
- PNG ルートは `src/renderSpeed.ts` に残してあるが import されていない (将来画像が必要になったら復活可)

## 未解決課題 / 次にやること
1. `npm install` して型エラーがないか確認
2. `npm run sim` で Simulator 上の表示を目視確認 (行幅 24 char の中央寄せが合っているか)
3. iOS Even Hub WebView で `navigator.geolocation` が本当に動くか実機検証
   - 動かない場合: SDK の `callEvenApp('getLocation', ...)` 系の存在調査 or HTTPS 必須の確認
4. 実機接続時の表示崩れを実機で調整
5. GitHub Pages デプロイ設定 (`vite.config.ts` の `base`) — 今は未設定

## 参考
- SDK 仕様: `../EVEN SDK仕様調査/04_SDK_API完全リファレンス.md`
- eventType=0 バグ: `../EVEN SDK仕様調査/12_制限事項と既知バグ.md`
- 実機テスト手順: `../onsa/実機テスト手順.md`
