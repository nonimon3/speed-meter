# SPEED METER 実行手順

## 前提
- Node.js v18+ / npm
- Windows の場合は **Git Bash** を使う (PowerShell では `.sh` が動かないことがある)
- 実機テストには iPhone + G2 + Even Realities アプリ

## 初回セットアップ
```bash
cd "D:/Download/Claude PJ/EVEN HUB/SPEED METER"
npm install
```

## Simulator で試す (GPS なしでもOK、Mock速度で動く)
```bash
npm run sim
```
- vite dev と `@evenrealities/evenhub-simulator` を同時起動
- Simulator は `http://localhost:5273/?mock=1&profile=city` を自動で開く → Mock 速度が流れる
- プロファイル指定: `SIM_PROFILE=highway npm run sim` / `SIM_PROFILE=walk npm run sim`
- 走行パターン:
  - **walk**: 4.5 km/h 前後の徒歩
  - **city**: 信号停止→加速→巡航(約45km/h)→減速の繰り返し
  - **highway**: 100〜120 km/h の巡航
- Simulator 上のタップで km/h ⇔ mph 切替、スクロールで Mock プロファイル切替

### Simulator を使わず素のブラウザで Mock を使う
```bash
npm run dev
# ブラウザで http://localhost:5273/?mock=1 を開く
```
または起動後に画面の `Mock: OFF` ボタンで ON にする。

## ブラウザだけで試す (GPS 動作確認)
```bash
npm run dev:https
```
- `https://localhost:5273/` を開く
- 初回アクセスで位置情報の許可ダイアログが出るので「許可」
- 屋内だと速度が取れないので、実際に移動するか Chrome DevTools の Sensors で座標を動かす
- G2 Bridge は無いので "G2 未接続（ブラウザモード）" と表示され、DOM 側だけが更新される

### HTTPS 証明書の警告
`@vitejs/plugin-basic-ssl` は自己署名なので、Chrome/Safari で一度 "詳細 → 安全でない方法で続行" を選んで許可しておく。

## iPhone の Even Hub アプリで実機テスト
```bash
# 1. HTTPS + QR を同時起動
npm run start:https
```
1. ターミナルに QR が表示される
2. iPhone で Even Realities アプリを開く → 右上メニュー → **QR Scan**
3. QR を読む → WebView で SpeedMeter が開く → G2 に配信される
4. **iOS 設定 → Even Hub → 位置情報 を事前に "使用中のみ許可"** にしておくこと

### 接続確認ポイント
- G2 画面に `SpeedMeter` と `-- km/h` が出れば Bridge 通信 OK
- `GPS: 取得待ち...` のまま変わらない場合は iOS 側の位置情報許可を確認
- 屋内は GPS を掴めないため、窓際か屋外に出る

## 操作
| 操作 | 動作 |
|------|------|
| シングルタップ | km/h ⇔ mph 切替 |
| ダブルタップ | アプリ終了 |
| ブラウザの「単位切替」ボタン | 同上 (DOM と G2 両方に反映) |
| ブラウザの「リセット」ボタン | 最大値/平均値をクリア |

## ビルド & パッケージ
```bash
npm run build      # dist/ に出力
npm run pack       # speed-meter.ehpk を生成
```

## よくあるトラブル
| 症状 | 対処 |
|------|------|
| `navigator.geolocation` が undefined | HTTPS で開いているか確認。iOS は Safari/WebView ともに非 HTTPS だと geolocation を拒否する |
| 位置情報ダイアログが出ない | iOS 設定 → Even Hub → 位置情報 を手動で有効化 |
| `sendFailed(4)` が出る | `createStartUpPageContainer` が走る前に何か送っていないか確認 (今回の実装では起動順は担保済み) |
| 速度が 0 から動かない | GPS が収束していない。屋外に出て数秒待つ、もしくはコンセプト確認は `?mock=1` を index.html に渡して GpsSpeedometer.startMock() を呼ぶよう仕込むと便利 |
| タップが反応しない | `isEventCapture: 1` を指定済。反応しない場合は SDK の eventType=0 バグを疑う → `src/g2.ts` の `resolveEventType` が拾っているはず |

## 開発中のチェックリスト
- [ ] `npm install` が通る
- [ ] `npm run dev:https` でブラウザから位置情報が取れる
- [ ] iPhone QR スキャンで G2 画面が出る
- [ ] 屋外で実速度が反映される
- [ ] タップで単位が切り替わる
- [ ] ダブルタップで終了する
